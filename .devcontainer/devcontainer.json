{
  "name": "secure-browser",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {},
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "forwardPorts": [3000],
  "portsAttributes": {
    "3000": { "label": "Browser", "onAutoForward": "openPreview" }
  },
  "postStartCommand": "nohup bash -c 'docker compose -f .devcontainer/docker-compose.yml up -d; for i in $(seq 1 240); do gh codespace ports visibility 3000:public -c \"$CODESPACE_NAME\" >/dev/null 2>&1 && { echo public; break; }; sleep 5; done' >/tmp/poststart.log 2>&1 &"
}
